<div class="container-fluid">

        <!-- Page Heading -->
        <h1 class="h3 mb-2 text-gray-800">Listado de Roles</h1>
        <!-- DataTales Example -->

        <div class="card shadow mb-4">
          <div class="card-header py-3">
            <div class="float-sm-right">
              <a href="<?php echo BASE_URL ?>rol/agregar/" class="btn btn-primary btn-icon-split">
                <span class="icon text-white-50">
                  <i class="fas fa-check"></i>
                </span>
                <span class="text">Registrar Roles</span>
              </a>
            </div>
          </div>
          <div class="card-body">
            <?php if(isset($this->datos) && count($this->datos)){?>
            <div class="table-responsive">
              <table class="table table-bordered" id="dataTableEsp" width="100%" cellspacing="0">
                <thead>
                  <tr>
                    <th>Descripci&oacute;n</th>
                    <th>Acciones</th>
                  </tr>
                </thead>
                <tbody>
                  <?php foreach ($this->datos as $key => $value) { ?>
                  <tr>
                    <td><?php echo $value->getDescripcion(); ?></td>
                    <td class="text-center">
                      <a title="Actualizar" href="<?php echo BASE_URL.$this->_presentRequest->getControlador().'/'; ?>actualizar/<?php echo $value->getId(); ?>/"><i class="fas fa-edit"></i></a>
                    </td>
                  </tr>
                  <?php } ?>
                </tbody>
              </table>
            </div>
            <?php }else{ ?>
              <div class="alert alert-warning" role="alert">
               No hay registros.
              </div>
            <?php } ?>
          </div>
        </div>

      </div>


</div>